---
name: <kebab-case-name>
description: <one line, third person - what it does AND when to use it>
license: Apache-2.0
---

# <Human Title>

<The organization's own instructions, preserved as they wrote them: the checklist,
standard, or playbook. Do NOT summarize, reorder, or reword. The only edits: remove
references to the old runtime ("return to the supervisor", "emit to the queue") and
framework wiring (Argo, kagents, LangGraph, CrewAI, k8s) that would mislead an agent.
Keep every business rule. If the source is long or multi-topic, split it into several
focused skills rather than trimming it.>
